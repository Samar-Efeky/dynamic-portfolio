{
  "version": 3,
  "framework": "angular",
  "buildCommand": "ng build && ng run PROJECT_NAME:server",
  "outputDirectory": "dist/dynamic-portfolio/browser",
  "routes": [
    { "src": "/.*", "dest": "/" }
  ]
}
